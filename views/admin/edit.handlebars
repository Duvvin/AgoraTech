<div class="card">
    <div class="card-body mb-3">
        <h2 class="mb-4">Editar Categoria</h2>
        <form action="/categorias/edit" method="POST" class="form-control">
            <input type="hidden" name="id" id="id" value="{{categoria._id}}">
            <label for="titulo" class="form-label">Titulo: </label>
            <input type="text" name="nome" id="nome" class="form-control" value="{{categoria.nome}}">
            <label for="conteudo" class="form-label">Conteúdo: </label>
            <input type="text" name="slug" id="slug" class="form-control" value="{{categoria.slug}}"></textarea>
            <button type="submit" class="btn btn-success mt-3">Concluido</button>
        </form>
    </div>
</div>