import type { MakeRequestOptionsWithoutMethod, RESTOptions } from './types';
/**
 * Represents the class that manages handlers for endpoints.
 */
export declare class REST {
    /**
     * Options to use in all requests.
     */
    readonly options: RESTOptions;
    constructor(
    /**
     * Options to use in all requests.
     */
    options?: RESTOptions);
    /**
     * Sets the authorization token that should be used for requests.
     * @param secret The secret to use.
     */
    setSecret(secret: string): this;
    /**
     * Runs a GET request from the API.
     */
    get<R>(route: string, options?: MakeRequestOptionsWithoutMethod): Promise<R>;
    /**
     * Runs a POST request from the API.
     */
    post<R>(route: string, options?: MakeRequestOptionsWithoutMethod): Promise<R>;
    /**
     * Runs a DELETE request from the API.
     */
    delete<R>(route: string, options?: MakeRequestOptionsWithoutMethod): Promise<R>;
    /**
     * Runs a PUT request from the API.
     */
    put<R>(route: string, options?: MakeRequestOptionsWithoutMethod): Promise<R>;
    /**
     * Runs a PATCH request from the API.
     */
    patch<R>(route: string, options?: MakeRequestOptionsWithoutMethod): Promise<R>;
    private makeRequest;
    private handleError;
    private process;
    private makeURL;
    private makeHeaders;
}
